#include<stdio.h>
int main()
{
    int x,sum=0;
    scanf("%d",&x);
    for(;x;scanf("%d",&x))
    {
        if(x>0)
        {
            sum=sum+x;
        }
    }
    printf("%d\n",sum);
    return 0;
}
